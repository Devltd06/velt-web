// CopilotToggle removed; kept placeholder to avoid accidental imports (feature was requested to be removed).
import React from 'react';
import { View } from 'react-native';

export default function CopilotToggle(_: { style?: any }) {
  return <View />;
}
